const TICKET_CONSTANTS = Object.freeze({
  name: Object.freeze({
    minLength: 5,
    maxLength: 200,
  }),
  description: Object.freeze({
    minLength: 5,
    maxLength: 2000,
  }),
});

export { TICKET_CONSTANTS };
